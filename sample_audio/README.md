# Sample audio

The task evaluation audio is not included in this repository.

The demo accepts mono 16 kHz WAV files. Ground-truth transcripts can optionally
be supplied through a JSONL manifest configured with `GROUND_TRUTH_JSONL`.
