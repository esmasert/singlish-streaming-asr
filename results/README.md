# Evaluation results

This directory contains the WER and latency summaries reported in the
submission.

Evaluation uses cache-aware streaming inference. Audio chunks are processed
sequentially while encoder and decoder state is preserved across the stream.
